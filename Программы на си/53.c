////»спользу€ созданные в предыдущих пунктах макросы дл€ работы с отдельными
////битами, создать программу, котора€ читает с клавиатуры однобайтовое беззнаковое
////целое a и маску Ц последовательность из 8 символов УXФ, У—Ф или УSФ.ѕервым
////вводитс€ младший бит маски, последним Ц старший.„исло a измен€етс€ в
////соответствии с маской : если в некотором бите маски стоит УXФ, то значение
////соответствующего бита a не измен€етс€, если маска содержит У—Ф, соответствующий
////бит числа a сбрасываетс€, если маска содержит УSФ, соответствующий бит числа a
////устанавливаетс€.Ќа экран в столбец вывод€тс€ : введенное число a в дес€теричной
////системе счислени€, a в двоичной системе счислени€, маска, результирующее число в
////двоичной системе счислени€ и результирующее число в дес€теричной системе
////счислени€.
//#include <locale.h>
//#pragma warning(disable : 4996)
//#include <stdio.h>
//
//// макросы дл€ работы с битами
//#define SET_BIT(x, n) ((x) | (1U << (n)))
//#define CLEAR_BIT(x, n) ((x) & ~(1U << (n)))
//#define CHECK_BIT(x, n) (((x) >> (n)) & 1U)
//
//void print_binary(unsigned char num) {
//    for (int i = 7; i >= 0; i--) {
//        printf("%d", (num >> i) & 1);
//    }
//}
//
//int main() {
//    setlocale(LC_ALL, "RUS");
//    unsigned char a;
//    char mask[9]; // маска из 8 символов + завершающий ноль
//
//    // ввод числа a
//    printf("¬ведите однобайтовое беззнаковое целое число (0-255): ");
//    scanf("%hhu", &a);
//
//    // ввод маски
//    printf("¬ведите маску из 8 символов (X, C, S): ");
//    scanf("%s", mask);
//
//    // вывод данных до изменени€
//    printf("\n–езультаты:\n");
//    printf("»сходное число a (дес€тичное): %d\n", a);
//    printf("»сходное число a (двоичное): ");
//    print_binary(a);
//
//    // изменение числа a в соответствии с маской
//    for (int i = 0; i < 8; i++) {
//        switch (mask[i]) {
//        case 'X':
//            // бит не измен€етс€
//            break;
//        case 'C':
//            a = CLEAR_BIT(a, i);
//            break;
//        case 'S':
//            a = SET_BIT(a, i);
//            break;
//        default:
//            printf("ќшибка: неверный символ в маске. »спользуйте только X, C, S.\n");
//            return 1;
//        }
//    }
//
//    // вывод результатов
//    
//    printf("\nћаска: %s\n", mask);
//    printf("–езультирующее число (двоичное): ");
//    print_binary(a);
//    printf("\n–езультирующее число (дес€тичное): %d\n", a);
//
//    return 0;
//}
