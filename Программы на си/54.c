////¬вести с клавиатуры x, использу€ указатель выделить и вывести на экран в столбик
////байты этого числа, начина€ со старшего Ц моделирование разделени€ числа на
////отдельные байты дл€ передачи по последовательному интерфейсу.
//#include <stdio.h>
//#include <locale.h>
//#pragma warning(disable : 4996)
//#include <stdlib.h>
//
//int main() {
//    setlocale(LC_ALL, "RUS");
//    unsigned int x; // вводимое число
//    unsigned char* bytes; // указатель на массив байтов
//
//    // ввод числа x
//    printf("¬ведите целое число (0-4294967295): ");
//    scanf("%u", &x);
//
//    // выделение пам€ти дл€ 4 байтов
//    bytes = (unsigned char*)malloc(4 * sizeof(unsigned char));
//    if (bytes == NULL) {
//        printf("ќшибка выделени€ пам€ти.\n");
//        return 1;
//    }
//
//    // разделение числа на байты
//    for (int i = 0; i < 4; i++) {
//        bytes[i] = (x >> (24 - i * 8)) & 0xFF; // извлечение каждого байта
//    }
//
//    // ¬ывод исходного числа в шестнадцатеричном представлении
//    printf("»сходное число: %u (в шестнадцатеричном представлении: 0x%X)\n", x, x);
//    // вывод байтов в столбик
//    printf("Ѕайты числа %u:\n", x);
//    for (int i = 0; i < 4; i++) {
//        printf("Ѕайт %d: %u\n", i + 1, bytes[i]);
//    }
//
//    // освобождение выделенной пам€ти
//    free(bytes);
//
//    return 0;
//}
