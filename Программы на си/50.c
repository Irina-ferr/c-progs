////Оформите в виде функции проверку состояния бита переменной n - го бита
////переменной x.На входе, соответственно, x и n, на выходе – логическое значение
////“истина”, в случае, если n - й бит установлен, “ложь” в противном случае.
//#include <stdio.h>
//#include <locale.h>
//#pragma warning(disable : 4996)
//
//// функция для проверки состояния n-го бита
//int is_bit_set(char x, int n) {
//    // проверяем, установлен ли n-й бит
//    return (x & (1 << n)) != 0; // возвращаем 1, если установлен, иначе 0
//}
//
//int main() {
//    setlocale(LC_ALL, "RUS");
//    char x;
//    int n;
//
//    // ввод значения x
//    printf("Введите значение x (в десятичном формате): ");
//    scanf("%hhd", &x); // используем %hhd для ввода char
//
//    // ввод индекса n
//    printf("Введите индекс n для проверки состояния бита: ");
//    scanf("%d", &n);
//
//    // проверка диапазона индекса n
//    if (n < 0 || n > 7) {
//        printf("Ошибка: индекс должен быть в диапазоне от 0 до 7.\n");
//        return 1; // завершаем программу с ошибкой
//    }
//
//    // проверяем состояние n-го бита и выводим результат
//    if (is_bit_set(x, n)) {
//        printf("n-й бит (%d) установлен.\n", n);
//    }
//    else {
//        printf("n-й бит (%d) не установлен.\n", n);
//    }
//
//    return 0;
//}
