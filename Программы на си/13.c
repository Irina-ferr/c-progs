//Резиновое кольцо диаметром d разрезали в n местах. Какова средняя длина получившихся кусков?
#include <stdio.h>
#include <math.h> 

int main() {
    float d; // диаметр кольца
    int n;   // количество разрезов

    // ввод диаметра кольца и количества разрезов
    printf("Введите диаметр кольца (d): ");
    scanf("%f", &d);
    printf("Введите количество разрезов (n): ");
    scanf("%d", &n);

    // диаметр кольца больше 0? количество разрезов не отрицательное?
    if (d <= 0) {
        printf("Ошибка: Диаметр кольца должен быть больше 0.\n");
        return 1; 
    }

    if (n < 0) {
        printf("Ошибка: Количество разрезов не может быть отрицательным.\n");
        return 1; 
    }

    // Вычисляем длину кольца
    float L = 3.1415926536 * d;

    // Вычисляем среднюю длину кусочков
    float average_length = L / (n + 1);

    // Выводим результат
    printf("Средняя длина получившихся кусков: %.2f\n", average_length);

    return 0;
}
