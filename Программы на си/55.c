////—оздать динамический массив M типа unsigned char.¬вести с клавиатуры
////байты числа x, сохран€€ их в M, старший байт вводитс€ первым.»спользу€ указатель
////объединить отдельные байты массива M в одно число и сохранить его в x.¬ывести
////значение x на экран, как переменную типа signed long int Ц моделирование
////Усклеивани€Ф числа из отдельных байтов при их приеме по последовательному
////интерфейсу.
//#include <stdio.h>
//#include <stdlib.h>
//#include <locale.h>
//#pragma warning(disable : 4996)
//int main() {
//    setlocale(LC_ALL, "RUS");
//    int numBytes; // количество байтов
//    unsigned char* M; // динамический массив дл€ хранени€ байтов
//    signed long int x = 0; // переменна€ дл€ хранени€ объединенного числа
//
//    printf("¬ведите количество байтов: ");
//    scanf("%d", &numBytes);
//
//    // выдел€ем пам€ть под массив M
//    M = (unsigned char*)malloc(numBytes * sizeof(unsigned char));
//    if (M == NULL) {
//        fprintf(stderr, "ќшибка выделени€ пам€ти\n");
//        return 1;
//    }
//
//    // вводим байты числа x, старший байт вводитс€ первым
//    printf("¬ведите %d байт(а) (в диапазоне 0-255):\n", numBytes);
//    for (int i = 0; i < numBytes; i++) {
//        printf("Ѕайт %d: ", i + 1);
//        scanf("%hhu", &M[i]); // вводим байт как unsigned char
//    }
//
//    // объедин€ем байты в одно число
//    for (int i = 0; i < numBytes; i++) {
//        x = (x << 8) | M[i]; // сдвигаем x влево и добавл€ем новый байт
//    }
//
//    // выводим значение x
//    printf("ќбъединенное число x: %ld\n", x);
//
//    // освобождаем выделенную пам€ть
//    free(M);
//
//    return 0;
//}
