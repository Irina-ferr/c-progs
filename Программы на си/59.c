////¬ текстовом редакторе создайте текстовый файл и запишите в этот файл
////произвольный текст.—оздайте программу, провер€ющую, нет ли среди этого текста
////последовательности символов вида : У[N = сссс]Ф, где сссс Ц целое число, которое
////может измен€тьс€ в диапазоне от - 100 до 100. ћежду знаком У = Ф и числом, а также
////между числом и символом У]Ф может быть произвольное количество пробелов и
////символов табул€ции.≈сли указанна€ последовательность обнаружена, вывести на
////экран считанное число, умноженное на 2. ≈сли последовательность не обнаружена,
////или если считанное значение выходит за границы указанного диапазона, то вывести
////на экран соответствующее сообщение.
//#include <stdio.h>
//#include <stdlib.h>
//#include <string.h>
//#include <ctype.h>
//#include <locale.h>
//#pragma warning(disable : 4996)
//
//#define MAX_LINE_LENGTH 256
//
//// функци€ дл€ пропуска пробелов и табул€ции
//void skipWhitespace(const char** ptr) {
//    while (isspace(**ptr)) {
//        (*ptr)++;
//    }
//}
//
//// основна€ программа
//int main() {
//    setlocale(LC_ALL, "RUS");
//    FILE* file;
//    char line[MAX_LINE_LENGTH];
//    int number;
//    int found = 0;
//
//    // открытие файла дл€ чтени€
//    file = fopen("59.txt", "r");
//    if (file == NULL) {
//        printf("ќшибка открыти€ файла.\n");
//        return 1;
//    }
//
//    // чтение построчно
//    while (fgets(line, sizeof(line), file)) {
//        const char* ptr = line;
//
//        // поиск "[N = "
//        while ((ptr = strstr(ptr, "[N = ")) != NULL) {
//            ptr += 5; // пропустить "[N = "
//
//            // пропуск пробелов и табул€ции
//            skipWhitespace(&ptr);
//
//            // чтение числа
//            if (sscanf(ptr, "%d", &number) == 1) {
//                // проверка границ
//                if (number >= -100 && number <= 100) {
//                    // поиск символа "]"
//                    ptr = strchr(ptr, ']');
//                    if (ptr == NULL) break;//чтобы не выводил значени€ без закрытой скобки
//                    printf("Ќайдено значение: %d, его удвоенное значение: %d\n", number, number * 2);
//                    found = 1;
//                }
//            }
//
//            // поиск символа "]"
//            ptr = strchr(ptr, ']');
//            if (ptr == NULL) break; // если не найдено, выходим из цикла
//        }
//    }
//
//    fclose(file);
//
//    // если ничего не найдено
//    if (!found) {
//        printf("ѕоследовательность не обнаружена или значение вне диапазона.\n");
//    }
//
//    return 0;
//}
