#include <locale.h>#include <stdio.h>#pragma warning(disable : 4996)#define SIZE 10int main() {    setlocale(LC_ALL, "RUS");    signed char arr[SIZE]; // объ€вление массива    // ¬вод массива    printf("¬ведите %d целых чисел (от -128 до 127):\n", SIZE);    for (int i = 0; i < SIZE; i++) {        printf("arr[%d]: ", i);        scanf("%hhd", &arr[i]); // используем %hhd дл€ чтени€ signed char    }    // ѕроверка на упор€доченность по убыванию    int isSortedDescending = 1; // флаг упор€доченности    for (int i = 0; i < SIZE - 1; i++) {        if (arr[i] < arr[i + 1]) { // если текущий элемент меньше следующего            isSortedDescending = 0; // устанавливаем флаг в 0            break; // прерываем цикл, так как пор€док нарушен        }    }    // ¬ывод результата    if (isSortedDescending) {        printf("Ёлементы массива упор€дочены по убыванию.\n");    }    else {        printf("Ёлементы массива не упор€дочены по убыванию.\n");    }    return 0;}