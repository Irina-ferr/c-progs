////—читать из текстового файла записанные в него последовательно и разделенные
////произвольным количеством пустых строк, пробелов и символов табул€ции 2 целых
////числа : первое Ц период сигнала в дискретных отсчетах - t, второе Ц длина сигнала в
////дискретных отсчетах - n.—генерировать меандр, измен€ющийс€ в диапазоне от 0 до
////1 с периодом t и длиной n дискретных отсчетов.—охранить сгенерированный
////сигнал в столбец в текстовый файл в виде последовательности целых чисел. (ћеандр
////	Ц последовательность пр€моугольных импульсов со скважностью 2, т.е.в нашем
////	случае пол периода значение такого сигнала 0, а следующие пол периода - 1).≈сли t
////	Ц нечетное или t < 2, или n < 2 то вывести сообщение об этом на экран.
//#include <stdio.h>
//#include <stdlib.h>
//#include <locale.h>
//#pragma warning(disable : 4996)
//int main() {
//    setlocale(LC_ALL, "RUS");
//    FILE* inputFile, * outputFile;
//    int t, n;
//
//    // открываем файл дл€ чтени€
//    inputFile = fopen("input.txt", "r");
//    if (inputFile == NULL) {
//        perror("Ќе удалось открыть файл дл€ чтени€");
//        return EXIT_FAILURE;
//    }
//
//    // считываем значени€ t и n, игнориру€ пустые строки и пробелы
//    while (fscanf(inputFile, "%d %d", &t, &n) != 2) {
//        // ѕропускаем некорректные строки
//        fscanf(inputFile, "%*[^\n]");
//    }
//
//    fclose(inputFile); // закрываем входной файл
//
//    // провер€ем услови€ дл€ t и n
//    if (t % 2 != 0 || t < 2 || n < 2) {
//        printf("ќшибка: t должно быть четным и больше или равно 2, n должно быть больше или равно 2.\n");
//        return EXIT_FAILURE;
//    }
//
//    // открываем файл дл€ записи
//    outputFile = fopen("output.txt", "w");
//    if (outputFile == NULL) {
//        perror("Ќе удалось открыть файл дл€ записи");
//        return EXIT_FAILURE;
//    }
//
//    // генерируем меандр и записываем в файл
//    for (int i = 0; i < n; i++) {
//        if ((i / (t / 2)) % 2 == 0) {
//            fprintf(outputFile, "0\n");
//        }
//        else {
//            fprintf(outputFile, "1\n");
//        }
//    }
//
//    fclose(outputFile); // закрываем выходной файл
//
//    printf("—игнал успешно сгенерирован и сохранен в output.txt\n");
//
//    return EXIT_SUCCESS;
//}
